import numpy as np
n,m=input().split()
n=int(n)
m=int(m)
#print(n)
#print(m)
matrix=[]
row=[]
for i in range(n):
    row=input().split()
    #print(row)
    row=[int(k) for k in row]
    matrix.append(row)
print(np.array(matrix))
